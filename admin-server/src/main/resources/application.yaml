server:
  port: 8090

spring:
  application:
    name: admin-server

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: true

logging:
  pattern:
    console: "%d{dd.MM.yyyy HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"