apiVersion: apps/v1
kind: Deployment
metadata:
  name: transaction-service
  namespace: banking-api
  labels:
    app: transaction-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: transaction-service
  template:
    metadata:
      labels:
        app: transaction-service
      annotations:
        prometheus.io/path: '/actuator/prometheus'
        prometheus.io/port: '8080'
        prometheus.io/scrape: 'true'
    spec:
      containers:
        - name: transaction-service
          image: devmeetup/transaction-service:1.5
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          env:
            - name: POSTGRES_USER
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: postgres-user
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: postgres-password
            - name: RABBITMQ_HOST
              value: "rabbitmq"
            - name: RABBITMQ_USER
              valueFrom:
                configMapKeyRef:
                  name: app-config
                  key: rabbitmq-user
            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: app-secret
                  key: rabbitmq-password
---
apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: banking-api
  labels:
    app: transaction-service
spec:
  ports:
    - port: 8080
      protocol: TCP
  selector:
    app: transaction-service